# (seed) — the machine's memory tree
The path is the meaning. At first boot, bin/agent-shell seeds people/ projects/
system/ system/capabilities/ inbox/ here. This file just ensures the dir exists
in git (flakes only see git-tracked files).
